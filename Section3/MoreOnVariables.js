let petName = "Hal";

// Will cause error if uncommented becasue we cannot declare a variable twice
// let petName = "Spot";

// But we can re-assign
petName = "BUZZ";
console.log(petName);

// Rules of variable names

// 1. No reserved words
// 2. Cannot start with number (can start with dollar sign, underscore or any a-zA-Z; can include all of these AND numbers)
// 3. Cannot include spaces

